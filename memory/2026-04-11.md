# 2026-04-11

## 09:33 - Tavily setup hoàn tất
- Plugin Tavily đã enabled
- API key Tavily đã cấu hình (không lưu plaintext trong memory)
- Provider mặc định: `tavily`
- Gateway đã restart, RPC probe ok

**Quy tắc mới:**
- Khi nhận yêu cầu liên quan **duyệt web / quy trình tự động**, ưu tiên dùng:
  - `tavily_search` / `tavily_extract` để tìm kiếm và lấy nội dung
  - `browser` tool để thao tác trực tiếp (click, form, screenshot)
