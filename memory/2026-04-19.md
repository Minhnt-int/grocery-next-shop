# 2026-04-19

## Grocery Next Shop

### Snapshot
- Hoàn thiện gần full storefront + admin CRUD + admin auth.
- Build Next.js pass, routes admin/news/products chạy được.
- DB hiện tại vẫn là SQLite (`prisma/dev.db`) trong lúc chờ migrate Postgres.

### Hoàn thành chính
- Frontend: hero slideshow, search autocomplete an toàn qua API, dropdown category, cart sidebar, news pages, SEO metadata cho product/news.
- Admin: login/logout JWT cookie, bảo vệ `/admin/*`, CRUD Products/Categories/Posts, quản lý Orders, trang Settings lưu file config.
- API + Prisma: đầy đủ route CRUD, seed products/categories/news.

### Vấn đề còn lại
1. Login admin: API trả 200 nhưng redirect sau login chưa ổn định.
2. Hydration warning ở badge cart (Header).
3. Chưa chuyển SQLite -> PostgreSQL.

### PostgreSQL migration status
- Host có sẵn PostgreSQL 17 service (`postgresql-x64-17`) và `psql.exe`.
- Kết nối bằng `postgres / 9e41cd` thất bại (`password authentication failed`).
- Runtime hiện tại không có quyền elevated để chỉnh auth config trong `C:\Program Files\PostgreSQL\17\data`.

### Next actions
- Tạo DB Postgres local (qua pgAdmin/psql có password đúng).
- Đổi Prisma datasource sang PostgreSQL, `db push`, generate, seed.
- Fix login redirect bug + cart hydration mismatch.

## Memory hygiene note
- Daily memory phải ngắn, chỉ giữ thông tin durable; chi tiết dự án đưa vào `memory/projects.md`.
- Tránh dump nguyên session vào daily file.

---
*Updated: 2026-04-19 14:20 ICT*
